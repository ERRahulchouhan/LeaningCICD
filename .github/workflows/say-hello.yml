name: Say Hello

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: self-hosted

    steps:
      - name: Say Hello
        run: echo "Hello Rahul! GitHub Action is running successfully."